{
  "document_type": "visa",
  "country": "IND",
  "_comment": "# APPROXIMATE \u2014 verify against real sample documents. Indian visas can be ETA or sticker, formats vary.",
  "required_fields": [
    "document_number",
    "date_of_expiry"
  ],
  "date_order_rules": [
    {
      "before_field": "date_of_issue",
      "after_field": "date_of_expiry",
      "rule": "date_of_issue must precede date_of_expiry"
    }
  ],
  "expiry_thresholds_days": {
    "expiring_soon": 30
  },
  "document_number_pattern": "^[A-Z0-9]{8,15}$"
}